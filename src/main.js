 import PageHeader from './components/page-header/index';
 let version = VERSION;
 console.log(`TDUI v${version}`);
 const TDUI = {
     PageHeader,
     version
 };
 export {
     PageHeader,
     version
 };
 export default TDUI;